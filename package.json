{
  "name": "husba-beads",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Premium product catalogue and enquiry website for HUSBA Beads.",
  "scripts": {
    "build": "node scripts/build-web.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview": "node scripts/serve.mjs dist 4173"
  },
  "engines": {
    "node": ">=20"
  }
}

