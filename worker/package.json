{
  "name": "husba-beads-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test ../tests/*.test.mjs",
    "deploy": "npx wrangler@4 deploy",
    "migrate:remote": "npx wrangler@4 d1 migrations apply husba-beads --remote"
  }
}
